1、模型存储
	saver = tf.train.Saver()
	with tf.Session() as sess:
		...
		saver.save(sess,"path/模型名.ckpt")

	
2、模型加载
	saver = tf.train.Saver()
        with tf.Session() as sess:
		...
		saver.restore(sess,"path/模型名.ckpt")

